# model checkpoints (1GB+ each, too big for git)
outputs/vit_best.pth
outputs/vit_last.pth

# large prediction logs
outputs/test_predictions.json
outputs/manual_test_predictions.json

# python cache
__pycache__/
*.pyc

# data folders (videos and face crops are too large for git)
data/
